# 多准则效用函数融合框架：从信息源到决策的统一设计

## TL;DR

你的 FastSLAM + OG 双地图架构恰好是一个**天然的多信息源系统**：粒子云提供位姿不确定性（协方差）、landmark 列表可构建共视图（图连通性）、OG 地图提供空间信息熵。本文提出一个**四层融合框架**——信息论层（Shannon/Rényi/MI）、最优设计层（D-opt/T-opt）、图论层（代数连通度/生成树数）、几何路径层——通过**自适应权重调度**统一为一个标量效用函数。关键创新在于揭示这些信息源背后的数学统一性：高斯微分熵与 D-optimality 仅差一个对数变换、图拉普拉斯与 Fisher 信息矩阵在 SLAM 中等价、Rényi 熵折扣将探索-利用平衡编码为位姿不确定性的函数。框架提供完整的 MATLAB 实现方案，预计 7-10 天完成核心开发。

---

## 1. 信息源层：你已有数据能提取什么？

在深入融合之前，必须先明确一个根本问题：**FastSLAM 粒子、landmark 列表、OG 地图这三类数据，各自蕴含了哪些可用于效用计算的信息？** 许多人只利用了 OG 地图做前沿检测，却忽略了粒子云和 landmark 列表中同样丰富的信息。下面的分析将展示，你的双地图系统实际上是一个**五维信息提取器**。

### 1.1 从 FastSLAM 粒子云提取的信息

每个时间步，FastSLAM 维护 $N$ 个粒子 $\{x_t^{[k]}, w_t^{[k]}\}_{k=1}^N$。这组加权样本本身就编码了**完整的位姿后验分布**，远非单一均值和协方差所能概括。

**位姿协方差矩阵 $\Sigma_r$** 可以从粒子云直接估计：

$$
\bar{x} = \sum_{k=1}^N w_t^{[k]} x_t^{[k]}, \quad \Sigma_r = \sum_{k=1}^N w_t^{[k]} (x_t^{[k]} - \bar{x})(x_t^{[k]} - \bar{x})^T
$$

这个 $3 \times 3$ 矩阵（$x, y, \theta$）是 TOED 准则的直接输入——你可以从中计算 D-opt、T-opt、A-opt、E-opt 中的任意一个。但粒子云还能提供更多：

**有效粒子数 $N_{eff}$** 量化了粒子退化的程度：

$$
N_{eff} = \frac{1}{\sum_{k=1}^N (w_t^{[k]})^2}
$$

当 $N_{eff} \ll N$ 时，粒子云集中在少数几个粒子上，意味着后验分布高度集中（位姿确定性高）或粒子退化严重。**将 $N_{eff}$ 作为辅助指标引入效用函数**，可以在粒子退化时触发保守的探索策略（优先回环而非深入未知区域），因为此时位姿估计的可靠性已经下降。

**粒子权重分布的 KLD** 量化了当前后验与均匀先验之间的偏离：

$$
D_{KL}(W \| U) = \sum_{k=1}^N w_t^{[k]} \ln(N \cdot w_t^{[k]})
$$

当 $D_{KL}$ 接近 0 时，所有粒子权重近似相等，后验分布"平坦"（不确定性大）；当 $D_{KL}$ 很大时，少数粒子占据了绝大部分权重，后验集中在小区域内。**$D_{KL}$ 可以作为探索-利用切换的触发器**——当 KLD 过低时，说明机器人对自己的位置没有信心，应该优先寻找回环来收紧后验。

### 1.2 从 Landmark 列表提取的信息

每个粒子维护的 landmark 列表 $L = \{(\mu_j, \Sigma_j)\}_{j=1}^M$ 不仅是定位的锚点，更是一个**可计算的图结构**。关键在于理解 landmark 之间的**空间关系**如何编码了地图的几何连通性。

**Landmark 共视图的构建**：如果两个 landmark 在同一帧扫描中同时被观测到（即它们的角间隔在传感器 FOV 内，且距离都在有效范围内），则在它们之间建立一条边。边的权重可以设为观测噪声的倒数（观测越可靠，边权重越大）。形式上，对于 landmark $i$ 和 $j$：

$$
w_{ij} = \begin{cases} \frac{1}{\sigma_i^2 + \sigma_j^2} & \text{if } i,j \text{ co-visible in same scan} \\ 0 & \text{otherwise} \end{cases}
$$

这个图的**拉普拉斯矩阵** $L = D - W$（$D$ 为度矩阵，$W$ 为权重矩阵）蕴含了丰富的拓扑信息。Khosoussi et al. [^17^] 证明，在 2D SLAM 中，图的代数连通度 $\lambda_2(L)$ 与 E-opt 准则直接相关，而生成树数量 $\tau(G) = \det(L_{reduced})$ 与 D-opt 相关。**这意味着，即使你没有显式的 pose graph SLAM 后端，仅从 landmark 共视图就能计算图论准则**——这是对 Placed & Castellanos [^19^] 框架的一个简化但有效的近似。

**Landmark 协方差矩阵的聚合** 也可以提供全局地图质量指标。所有 landmark 的迹（trace）之和反映了地图的平均不确定性：

$$
\Psi_{map} = \frac{1}{M} \sum_{j=1}^M \text{tr}(\Sigma_j)
$$

这个指标可以与 OG 地图熵形成互补——OG 熵告诉你"空间上哪里还不确定"，而 landmark 协方差迹告诉你"已经观测到的 landmark 定位精度如何"。

### 1.3 从 OG 地图提取的信息

OG 地图是最直接的信息源，但同样可以从中提取多层次的信息：

**Shannon 熵** 是最基础的指标：

$$
H_{map} = -\sum_{c \in \mathcal{G}} \left[ p(c) \ln p(c) + (1-p(c)) \ln(1-p(c)) \right]
$$

其中 $\mathcal{G}$ 是 OG 地图的单元格集合。**但这里的创新点在于：不需要计算整个地图的熵**。对于每个候选目标点，只计算该点传感器覆盖区域内的熵，即**局部条件熵**：

$$
H_{map}(a) = -\sum_{c \in \text{FOV}(a)} \left[ p(c) \ln p(c) + (1-p(c)) \ln(1-p(c)) \right]
$$

这将计算复杂度从 $O(W \times H)$ 降低到 $O(\pi r^2 / \text{res}^2)$，对于一个射程 3m、分辨率 0.1m 的激光雷达，每次只需计算约 2800 个单元格而非整个地图。

**前沿熵密度** 是另一个有用的衍生指标。对于每个前沿候选点，可以计算其邻域内的**平均单元格熵**：

$$
\bar{h}(a) = \frac{H_{map}(a)}{|\text{FOV}(a)|}
$$

这个归一化指标消除了不同 FOV 大小带来的偏差，使得不同距离的前沿点可以公平比较。

**路径安全熵**（Ahmed et al. [^7^] 的思路）量化了从当前位置到候选目标的直线路径上的不确定性：

$$
H_{path}(a) = -\sum_{c \in \text{Bresenham}(x_r, x_a)} \left[ p(c) \ln p(c) + (1-p(c)) \ln(1-p(c)) \right]
$$

这条路径上的高熵意味着路径穿过大量未知区域（风险高），低熵意味着路径大部分是已知自由空间（安全）。**将 $H_{path}$ 作为负项加入效用函数，可以天然实现避障和保守导航**。

### 1.4 五种信息源汇总

| 信息源 | 原始数据 | 提取指标 | 计算复杂度 | 物理意义 |
|--------|---------|---------|-----------|---------|
| 粒子云 | $N$ 个加权位姿样本 | $\Sigma_r$, $N_{eff}$, $D_{KL}$ | $O(N)$ | 位姿后验分布的完整刻画 |
| Landmark 坐标 | $M$ 个 $(x,y)$ 点 | 共视图 $G$, $\lambda_2(L)$, $\bar{d}$ | $O(M^2)$ 建图, $O(M^3)$ 特征值 | 地图拓扑连通性 |
| Landmark 协方差 | $M$ 个 $2 \times 2$ 矩阵 | $\Psi_{map} = \frac{1}{M}\sum \text{tr}(\Sigma_j)$ | $O(M)$ | 已观测 landmark 的精度 |
| OG 地图 | $W \times H$ 概率网格 | $H_{map}(a)$, $\bar{h}(a)$, $H_{path}(a)$ | $O(\pi r^2/\text{res}^2)$ | 空间信息不确定性 |
| 几何关系 | 候选目标位置 | $\|x_r - x_a\|_2$, 路径长度 | $O(1)$ | 运动代价 |

---

## 2. 准则计算层：四大类方法的完整实现

有了上述五种信息源，现在可以构建四个准则层。每一层都从不同的信息子集出发，输出一个标量值。**关键洞察是：这些准则并非独立，它们之间存在深刻的数学联系**（见第 3 节）。

### 2.1 信息论准则层（$U_{IT}$）

信息论层从 OG 地图和粒子云提取信息，输出反映"预期信息增益"的标量。这一层融合 Shannon 熵、Rényi 熵折扣和互信息三种思想。

**基础形式**——Shannon 熵差（Carrillo et al. [^3^] 的简化版）：

$$
U_{IT}^{(base)}(a) = H_{map}(a) - \beta \cdot H_{pose}
$$

其中 $H_{map}(a)$ 是候选点 $a$ 的局部地图熵（越大越好，未知区域多），$H_{pose} = \frac{1}{2}\ln((2\pi e)^3 \det(\Sigma_r))$ 是当前位姿的微分熵（作为惩罚项，位姿越不确定惩罚越大），$\beta$ 是平衡系数。

**Rényi 熵折扣**——这是 Carrillo et al. [^3^] 的核心创新。不直接减去位姿熵，而是用一个**位姿不确定性依赖的 Rényi 参数** $\alpha$ 对地图熵进行折扣：

$$
U_{IT}^{(renyi)}(a) = H_{map}(a) - H_{\alpha}(a), \quad \alpha = 1 + f(\sigma_{pose})
$$

其中 $H_\alpha$ 是 Rényi 熵：

$$
H_\alpha[P] = \frac{1}{1-\alpha} \ln \sum_{c \in \text{FOV}(a)} p(c)^\alpha
$$

**关键性质**：当 $1 < \alpha \leq \alpha'$ 时，$H[P] > H_\alpha[P] \geq H_{\alpha'}[P] \geq 0$。这意味着 $\alpha$ 越大（位姿越不确定），Rényi 折扣越激进，预期信息增益越小，从而**自动降低深入未知区域的倾向**——这正是探索-利用平衡所需的。

$\alpha$ 与位姿不确定性的映射函数 $f(\cdot)$ 可以取多种形式，Carrillo 原文使用线性映射 $\alpha = 1 + \sigma_{pose}$，其中 $\sigma_{pose}$ 是通过 D-opt 从协方差矩阵提取的标量。但你可以尝试更丰富的形式：

| 映射函数 | 公式 | 特性 |
|---------|------|------|
| 线性 | $\alpha = 1 + k \cdot \sigma_{pose}$ | 简单直观，原论文使用 |
| 指数 | $\alpha = 1 + e^{k \cdot \sigma_{pose}} - 1$ | 不确定性高时折扣更激进 |
| Sigmoid | $\alpha = 1 + \alpha_{max} / (1 + e^{-k(\sigma_{pose} - \sigma_0)})$ | 平滑过渡，避免突变 |
| 对数 | $\alpha = 1 + \ln(1 + k \cdot \sigma_{pose})$ | 不确定性低时敏感，高时饱和 |

**MI 近似**——严格互信息需要预测未来观测，计算昂贵。一个实用的近似是**条件熵差**：

$$
U_{IT}^{(MI)}(a) \approx H_{map}(a) - \mathbb{E}[H_{map}(a \mid x_{t+1})]
$$

其中 $x_{t+1}$ 是通过运动模型从当前位姿传播到候选目标的预测位姿。**在 FastSLAM 中，这个期望可以直接用粒子的预测位姿来近似**——每个粒子传播到候选目标，计算其对应的局部熵，然后加权平均。

### 2.2 最优设计准则层（$U_{TOED}$）

TOED 层从位姿协方差矩阵 $\Sigma_r$ 提取信息，输出反映"后验精度"的标量。这一层直接利用 Kiefer 的 $p$-范数框架 [^68^]，但你不需要实现全部四种准则——**D-opt 和 T-opt 的组合已经足够丰富**。

**D-optimality**（协方差椭球体积）：

$$
D\text{-opt}(\Sigma_r) = \exp\left(\frac{1}{3} \sum_{k=1}^3 \ln \lambda_k\right) = \left(\det(\Sigma_r)\right)^{1/3}
$$

对于 $3 \times 3$ 的位姿协方差矩阵，特征值 $(\lambda_1, \lambda_2, \lambda_3)$ 分别对应 $x, y, \theta$ 方向的不确定性。D-opt 是它们的**几何平均**，对极端值不敏感。

**T-optimality**（平均方差）：

$$
T\text{-opt}(\Sigma_r) = \frac{1}{3} \sum_{k=1}^3 \lambda_k = \frac{1}{3} \text{tr}(\Sigma_r)
$$

T-opt 是特征值的**算术平均**，计算更简单（只需迹），但对大特征值更敏感。在探索初期，T-opt 能更快反映位姿漂移。

**融合形式**：

$$
U_{TOED} = w_D \cdot \left(1 - \frac{D\text{-opt}}{D\text{-opt}_{max}}\right) + w_T \cdot \left(1 - \frac{T\text{-opt}}{T\text{-opt}_{max}}\right)
$$

归一化到 $[0, 1]$ 范围后，$U_{TOED}$ 越大表示位姿不确定性越小（越"优"）。**注意符号**：在 TOED 中，协方差越小越优，所以效用应该是"1 减去归一化值"，或者直接将 $-D\text{-opt}$ 作为负惩罚项。

**MATLAB 实现**（从粒子云提取）：

```matlab
function [D_opt, T_opt] = compute_toed_criteria(particles)
    % 从粒子云估计位姿协方差
    poses = reshape([particles.pose], 3, []);  % 3 x N
    weights = [particles.weight];
    
    mean_pose = sum(poses .* weights, 2);  % 加权平均
    centered = poses - mean_pose;
    Sigma_r = (centered .* weights) * centered';  % 3x3 协方差矩阵
    
    % 特征值分解
    eigenvals = eig(Sigma_r);  % [lambda_x, lambda_y, lambda_theta]
    
    % D-opt (几何平均的指数形式)
    D_opt = exp(mean(log(eigenvals)));  % = det(Sigma_r)^(1/3)
    
    % T-opt (算术平均 = 迹/3)
    T_opt = mean(eigenvals);  % = trace(Sigma_r)/3
end
```

### 2.3 图论准则层（$U_{Graph}$）

图论层从 landmark 共视图提取信息，输出反映"地图拓扑质量"的标量。这一层的核心洞察来自 Khosoussi et al. [^17^]：**SLAM 的 pose graph 本质上是一个加权图，其连通性与位姿估计的不确定性直接相关**。

**Landmark 共视图构建**：

```matlab
function [L, lambda2, avg_degree] = build_landmark_covisibility_graph(...
    landmarks, covariances, robot_pose, sensor_fov, sensor_range)
    % landmarks: 2 x M 矩阵
    % covariances: 2 x 2 x M
    
    M = size(landmarks, 2);
    W = zeros(M, M);  % 权重矩阵
    
    for i = 1:M
        for j = i+1:M
            % 计算从机器人看两个landmark的夹角
            v_i = landmarks(:, i) - robot_pose(1:2);
            v_j = landmarks(:, j) - robot_pose(1:2);
            
            d_i = norm(v_i);
            d_j = norm(v_j);
            
            % 检查是否都在传感器范围内
            if d_i < sensor_range && d_j < sensor_range
                angle_diff = abs(atan2(v_i(2), v_i(1)) - atan2(v_j(2), v_j(1)));
                angle_diff = min(angle_diff, 2*pi - angle_diff);
                
                if angle_diff < sensor_fov/2  % 共视
                    % 边权重 = 观测精度的函数
                    w_ij = 1 / (trace(covariances(:,:,i)) + trace(covariances(:,:,j)));
                    W(i, j) = w_ij;
                    W(j, i) = w_ij;
                end
            end
        end
    end
    
    % 度矩阵和拉普拉斯矩阵
    D = diag(sum(W, 2));
    L = D - W;
    
    % 代数连通度（第二小特征值）
    eigenvals_L = sort(eig(L));
    lambda2 = eigenvals_L(2);  % Fiedler值
    
    % 平均节点度
    avg_degree = mean(sum(W > 0, 2));
end
```

**为什么这有用？** 代数连通度 $\lambda_2(L)$ 被称为图的"Fiedler 值"，它量化了图的"连通强度"。在 SLAM 中：

- **$\lambda_2$ 大** → landmark 之间有大量强连接 → 位姿估计约束充分 → 不确定性低 → 可以放心探索
- **$\lambda_2$ 小** → 图接近断开 → 某些 landmark 孤立观测 → 位姿估计脆弱 → 应该优先建立连接（回环）

Placed & Castellanos [^19^] 证明，在 $SE(n)$ 上，$\lambda_2(L)$ 与 E-opt 准则存在解析关系；Khosoussi et al. [^17^] 进一步证明 $\tau(G) = \det(L_{reduced}) \propto D\text{-opt}^{-1}$。这意味着**图论准则和 TOED 准则在数学上是等价的，只是计算路径不同**——图论路径避免了协方差矩阵求逆，计算更高效。

**融合形式**：

$$
U_{Graph} = \lambda_2(L) + w_d \cdot \bar{d}
$$

其中 $\bar{d}$ 是平均节点度。$\lambda_2$ 是主要指标，$\bar{d}$ 提供补充信息。

### 2.4 几何与路径准则层（$U_{Geo}$）

几何层是最简单但不可或缺的——它确保机器人不会因为追求信息增益而做出荒谬的决策（比如选择 100 米外的目标点）。

**距离惩罚**：

$$
U_{Geo}^{(dist)}(a) = -\|x_r - x_a\|_2
$$

**路径安全熵**（Ahmed et al. [^7^]）：

$$
U_{Geo}^{(path)}(a) = -\sum_{c \in \text{Bresenham}(x_r, x_a)} h(c)
$$

其中 $h(c) = -p(c)\ln p(c) - (1-p(c))\ln(1-p(c))$ 是单个单元格的熵。**路径上的高熵意味着路径穿过大量未知区域——这是一个"安全惩罚"**，与距离惩罚形成互补：距离惩罚关心"远不远"，路径熵惩罚关心"安不安全"。

**完整几何层**：

$$
U_{Geo}(a) = -w_{dist} \cdot \|x_r - x_a\|_2 - w_{path} \cdot H_{path}(a) - w_{turn} \cdot |\Delta\theta|
$$

其中 $|\Delta\theta|$ 是朝向变化量，惩罚大角度转向（能量消耗）。

---

## 3. 数学统一：为什么这些方法可以融合

四种准则层看似来自不同的数学分支（信息论、矩阵论、图论、几何学），但 Placed et al. [^68^] 的综述揭示了一个深刻的统一视角：**它们都在量化"不确定性"，只是用了不同的数学语言**。

### 3.1 信息论 ↔ 最优设计：对数桥梁

对于高斯分布，微分熵与 D-optimality 之间存在**精确的对数关系** [^68^]：

$$
H_{\mathcal{N}}(\Sigma) = \frac{1}{2} \ln\left((2\pi e)^n \det(\Sigma)\right) = \frac{n}{2} \ln(2\pi e) + \frac{n}{2} \ln(D\text{-opt})
$$

这意味着 $H_{\mathcal{N}} \propto \ln(D\text{-opt})$，两者**单调等价**——最大化 D-opt 等价于最大化高斯微分熵。这就是为什么在位姿高斯假设下，信息论的 $H_{pose}$ 和 TOED 的 $D\text{-opt}(\Sigma_r)$ 本质上是同一信息的不同编码。

**实践意义**：在融合框架中，你不需要同时计算 $H_{pose}$ 和 $D\text{-opt}$ 作为两个独立项。可以任选其一作为主指标，另一个作为"校验"。或者，更巧妙的做法是用 D-opt 计算 Rényi 参数 $\alpha$，再用 Rényi 熵折扣地图信息——这就是 Carrillo 方法的本质 [^3^]。

### 3.2 图论 ↔ 最优设计：拉普拉斯 ↔ FIM

Khosoussi et al. [^17^] 的核心定理建立了图拉普拉斯与 Fisher 信息矩阵（FIM）之间的桥梁。在 2D SLAM 中，如果所有边的测量噪声相同，则：

$$
\tau(G) = \det(L_{reduced}) \propto D\text{-opt}^{-1}, \quad \lambda_2(L) \propto E\text{-opt}
$$

这意味着：**从 landmark 共视图计算的图连通性指标，与从协方差矩阵计算的 TOED 指标，在数学上是等价的**。图论路径的优势在于计算效率——对于 $M$ 个 landmark，计算 $\lambda_2(L)$ 的复杂度是 $O(M^3)$，而直接求逆 $M \times M$ 的协方差矩阵也是 $O(M^3)$，但当图是稀疏时（landmark 只与附近的共视），拉普拉斯矩阵的稀疏结构可以显著加速特征值计算。

### 3.3 Rényi 折扣：连接所有层的"旋钮"

Rényi 参数 $\alpha$ 可以被视为一个**统一调节器**。当位姿不确定性（通过 D-opt 或 $\lambda_2$ 量化）变化时，$\alpha$ 自动调整，从而影响信息论层的输出。这形成了一个**跨层反馈**：

$$
\sigma_{pose} \xrightarrow{D\text{-opt}} \alpha \xrightarrow{R\acute{e}nyi} U_{IT} \xrightarrow{融合} U_{total}
$$

### 3.4 统一公式

将所有关系整合，融合效用函数可以写成：

$$
U(a, t) = \underbrace{\omega_{IT}(t) \cdot \left[H_{map}(a) - H_{\alpha(a)}(a)\right]}_{\text{信息论层}} + \underbrace{\omega_{TOED}(t) \cdot \left[-\ln D\text{-opt}(\Sigma_r)\right]}_{\text{TOED层}} + \underbrace{\omega_{Graph}(t) \cdot \lambda_2(L)}_{\text{图论层}} + \underbrace{\omega_{Geo}(t) \cdot \left[-d(a) - H_{path}(a)\right]}_{\text{几何层}}
$$

其中 $\alpha(a) = 1 + f(D\text{-opt}(\Sigma_r^{(a)}))$，$\Sigma_r^{(a)}$ 是预测执行动作 $a$ 后的位姿协方差。

---

## 4. 完整 MATLAB 融合框架实现

### 4.1 核心融合函数

```matlab
%% ========================================================================
% 多准则效用函数融合框架 - 核心计算
% 输入: particles (FastSLAM粒子数组)
%       og_map (occupancyMap对象)
%       candidate_goals (K x 2 候选目标点)
%       t (当前时间步)
% 输出: utilities (K x 1 效用值), best_idx (最优候选索引)
%% ========================================================================

function [utilities, best_idx, criteria] = multi_criteria_utility_fusion(...
    particles, og_map, candidate_goals, robot_pose, params)

    K = size(candidate_goals, 1);
    utilities = zeros(K, 1);
    
    % --- 预计算：从粒子云提取全局信息 (只需计算一次) ---
    [Sigma_r, D_opt_global, T_opt_global] = extract_pose_info(particles);
    [L, lambda2, avg_deg] = build_landmark_graph_from_best_particle(...
        particles, robot_pose, params);
    
    N_eff = 1 / sum([particles.weight].^2);
    R_unknown = compute_unknown_ratio(og_map);
    
    % --- 自适应权重调度 ---
    [w_IT, w_TOED, w_Graph, w_Geo] = adaptive_weight_scheduler(...
        R_unknown, D_opt_global, N_eff, params);
    
    % --- Rényi参数 (基于全局位姿不确定性) ---
    alpha_global = 1 + params.k_alpha * log(1 + D_opt_global);
    
    % --- 对每个候选目标计算多层效用 ---
    for i = 1:K
        goal = candidate_goals(i, :);
        
        % ===== 信息论层 U_IT =====
        % 1a: 候选点的局部地图熵
        H_map = compute_local_map_entropy(og_map, goal, params.sensor_range);
        
        % 1b: Rényi折扣 (用预测位姿不确定性)
        predicted_pose = predict_pose(robot_pose, goal);
        predicted_cov = predict_covariance(Sigma_r, robot_pose, goal);
        D_opt_pred = det(predicted_cov)^(1/3);
        alpha_pred = 1 + params.k_alpha * log(1 + D_opt_pred);
        H_renyi = compute_renyi_entropy_local(og_map, goal, params.sensor_range, alpha_pred);
        
        U_IT = H_map - H_renyi;
        
        % ===== TOED层 U_TOED =====
        % 使用预测位姿的D-opt作为"该动作后的预期不确定性"
        U_TOED = -log(D_opt_pred + 1e-6);  % 负对数，越小越优
        
        % ===== 图论层 U_Graph =====
        % 预测执行动作后图的代数连通度变化
        % 简化: 假设到达目标后看到新landmark，图连通度增加
        lambda2_pred = predict_graph_connectivity(L, goal, og_map, params);
        U_Graph = lambda2_pred;
        
        % ===== 几何层 U_Geo =====
        dist_cost = norm(robot_pose(1:2) - goal');
        H_path = compute_path_entropy(og_map, robot_pose(1:2), goal);
        delta_theta = abs(atan2(goal(2)-robot_pose(2), goal(1)-robot_pose(1)) - robot_pose(3));
        delta_theta = min(delta_theta, 2*pi - delta_theta);
        
        U_Geo = -params.w_dist*dist_cost - params.w_path*H_path - params.w_turn*delta_theta;
        
        % ===== 融合 =====
        utilities(i) = w_IT*U_IT + w_TOED*U_TOED + w_Graph*U_Graph + w_Geo*U_Geo;
        
        % 保存各准则值用于分析
        criteria.IT(i) = U_IT;
        criteria.TOED(i) = U_TOED;
        criteria.Graph(i) = U_Graph;
        criteria.Geo(i) = U_Geo;
    end
    
    [~, best_idx] = max(utilities);
end

%% ========================================================================
% 辅助函数
%% ========================================================================

function [Sigma_r, D_opt, T_opt] = extract_pose_info(particles)
    poses = reshape([particles.pose], 3, []);
    weights = [particles.weight];
    
    mean_pose = sum(poses .* weights, 2);
    centered = poses - mean_pose;
    Sigma_r = (centered .* weights) * centered' + 1e-6*eye(3);
    
    eigenvals = eig(Sigma_r);
    D_opt = exp(mean(log(max(eigenvals, 1e-10))));
    T_opt = mean(eigenvals);
end

function alpha = compute_renyi_entropy_local(og_map, pose, sensor_range, alpha_param)
    [cells_x, cells_y] = get_fov_cells(og_map, pose, sensor_range);
    probs = getOccupancy(og_map, [cells_x', cells_y']);
    
    % 只计算不确定单元格
    valid = (probs > 0.01) & (probs < 0.99);
    probs = probs(valid);
    
    if isempty(probs) || alpha_param == 1
        H = 0;
    else
        H = 1/(1-alpha_param) * log(sum(probs.^alpha_param));
    end
    alpha = H;
end

function [w_IT, w_TOED, w_Graph, w_Geo] = adaptive_weight_scheduler(...
    R_unknown, D_opt, N_eff, params)
    
    % 基础权重 (随探索进度)
    if R_unknown > 0.6
        base = [0.50, 0.15, 0.15, 0.20];  % 探索阶段: IT主导
    elseif R_unknown > 0.2
        base = [0.30, 0.25, 0.25, 0.20];  % 平衡阶段
    else
        base = [0.15, 0.40, 0.25, 0.20];  % 利用阶段: TOED+Graph主导
    end
    
    % 粒子退化修正 (N_eff低时增加保守性)
    degradation_factor = min(1, N_eff / (params.N_particles * 0.3));
    if degradation_factor < 0.5
        % 粒子退化严重: 增加几何权重(短路径优先)，降低IT权重
        base = base + [-0.15, 0.05, 0.05, 0.05];
    end
    
    % 位姿不确定性修正 (D_opt大时增加Graph权重)
    uncertainty_factor = min(1, D_opt / params.D_opt_threshold);
    if uncertainty_factor > 0.7
        base = base + [-0.10, 0.05, 0.10, -0.05];
    end
    
    % 归一化
    total = sum(base);
    w_IT = base(1) / total;
    w_TOED = base(2) / total;
    w_Graph = base(3) / total;
    w_Geo = base(4) / total;
end

function H_path = compute_path_entropy(og_map, start_pos, end_pos)
    [s_i, s_j] = world2grid(og_map, start_pos(1), start_pos(2));
    [e_i, e_j] = world2grid(og_map, end_pos(1), end_pos(2));
    [cells_i, cells_j] = bresenham_line(s_i, s_j, e_i, e_j);
    
    H_path = 0;
    for c = 1:length(cells_i)
        p = getOccupancy(og_map, [cells_j(c), cells_i(c)]);
        if p > 0.01 && p < 0.99
            H_path = H_path + (-p*log(p) - (1-p)*log(1-p));
        end
    end
end
```

### 4.2 主循环中的融合调用

```matlab
%% 主动SLAM主循环 - 多准则融合版
while ~exploration_complete
    % 1. 获取传感器数据
    [ranges, angles] = get_laser_scan();
    u_t = get_odometry();
    
    % 2. FastSLAM更新 (landmark SLAM后端)
    particles = fastslam_step(particles, u_t, ranges, angles);
    best_pose = get_best_pose(particles);
    
    % 3. OG地图更新
    global_og_map = update_occupancy_grid(global_og_map, ranges, angles, best_pose);
    
    % 4. 前沿检测
    frontiers = detect_frontiers_og(global_og_map);
    if isempty(frontiers), break; end
    
    % 5. ===== 多准则效用融合 (核心!) =====
    candidate_goals = extract_frontier_centers(frontiers);
    [utilities, best_idx, criteria] = multi_criteria_utility_fusion(...
        particles, global_og_map, candidate_goals, best_pose, params);
    
    % 6. 执行最优动作
    best_goal = candidate_goals(best_idx, :);
    send_goal_to_ros(best_goal);
    
    % 7. 记录分析数据
    log_criteria(t, criteria, best_idx);
    
    t = t + 1;
end
```

---

## 5. 参数调优指南与消融实验设计

### 5.1 关键参数表

| 参数 | 符号 | 推荐范围 | 调优建议 |
|------|------|---------|---------|
| Rényi映射系数 | $k_\alpha$ | [0.5, 3.0] | 越大，位姿不确定性对折扣影响越激进 |
| 距离权重 | $w_{dist}$ | [0.1, 0.5] | 环境大时调小，环境小时调大 |
| 路径熵权重 | $w_{path}$ | [0.05, 0.3] | 障碍物多/未知区域危险时调大 |
| 转向权重 | $w_{turn}$ | [0.01, 0.1] | 差速小车可设小，阿克曼转向车设大 |
| 探索-平衡阈值 | $\theta_{high}$ | [0.5, 0.7] | 环境复杂度高时调低 |
| 平衡-利用阈值 | $\theta_{low}$ | [0.15, 0.3] | 地图分辨率低时调高 |
| D-opt退化阈值 | $D_{opt,thr}$ | 根据实际标定 | 观察正常探索时的D-opt范围 |

### 5.2 消融实验设计

为了验证融合框架中各组件的贡献，建议设计以下消融实验：

| 实验组 | 启用的准则层 | 目的 |
|--------|-------------|------|
| Baseline | 仅 $U_{Geo}$ (距离) | 验证主动决策的必要性 |
| IT-only | $U_{IT} + U_{Geo}$ | 验证信息论层的独立贡献 |
| TOED-only | $U_{TOED} + U_{Geo}$ | 验证TOED层的独立贡献 |
| Graph-only | $U_{Graph} + U_{Geo}$ | 验证图论层的独立贡献 |
| IT+TOED | $U_{IT} + U_{TOED} + U_{Geo}$ | 验证IT与TOED的互补性 |
| IT+Graph | $U_{IT} + U_{Graph} + U_{Geo}$ | 验证IT与Graph的互补性 |
| **Full Fusion** | **全部四层 + 自适应权重** | **验证完整框架的优越性** |

**评价指标**：
- **探索效率**：覆盖一定比例环境所需的步数/时间
- **定位精度**：最终位姿估计与真值的 RMSE
- **地图质量**：OG地图与真值地图的互信息
- **计算开销**：每步效用计算的平均耗时

---

## 6. 两周实现计划（多准则融合版）

| 天数 | 任务 | 产出 |
|------|------|------|
| Day 1-2 | 在Tim Bailey FastSLAM基础上集成OG地图 | 可运行的FastSLAM+OG双地图系统 |
| Day 3 | 实现信息论层（Shannon熵 + Rényi折扣） | $U_{IT}$ 计算函数 |
| Day 4 | 实现TOED层（D-opt/T-opt从粒子云提取） | $U_{TOED}$ 计算函数 |
| Day 5 | 实现图论层（landmark共视图 + 代数连通度） | $U_{Graph}$ 计算函数 |
| Day 6 | 实现几何层（距离 + 路径熵 + 转向） | $U_{Geo}$ 计算函数 + Bresenham |
| Day 7 | **融合层**：自适应权重调度 + 多准则组合 | 完整 `multi_criteria_utility_fusion` 函数 |
| Day 8 | 与队友ROS/Gazebo联调 | 端到端可运行系统 |
| Day 9-10 | 消融实验（7组对比） | 实验数据 + 初步分析 |
| Day 11-12 | 参数调优 + 补充实验 | 最优参数组合 |
| Day 13 | 可视化 + 结果整理 | 图表 + 分析结论 |
| Day 14 | 报告撰写 + 演示准备 | 最终交付 |

这个时间表比简单加权方案多了 2-3 天，但产出的是一个**真正融合四种数学工具**的框架，在学术价值和工程深度上都有显著提升。

---

## 7. 核心公式速查

| 准则层 | 核心公式 | 信息源 | 关键参数 |
|--------|---------|--------|---------|
| **$U_{IT}$** | $H_{map}(a) - H_{\alpha(a)}(a)$, $\alpha = 1 + k_\alpha \ln(1 + D\text{-opt})$ | OG地图 + 粒子协方差 | $k_\alpha$ |
| **$U_{TOED}$** | $-\ln D\text{-opt}(\Sigma_r^{(a)})$ | 粒子协方差 | 无额外参数 |
| **$U_{Graph}$** | $\lambda_2(L)$ (Fiedler值) | Landmark共视图 | 共视阈值 |
| **$U_{Geo}$** | $-w_d d - w_p H_{path} - w_t |\Delta\theta|$ | OG地图 + 几何 | $w_d, w_p, w_t$ |
| **融合** | $U = \omega_{IT}U_{IT} + \omega_{TOED}U_{TOED} + \omega_{Graph}U_{Graph} + \omega_{Geo}U_{Geo}$ | 全部 | 自适应权重函数 |

**统一关系**：

$$
\underbrace{H_{\mathcal{N}}(\Sigma) \propto \ln(D\text{-opt})}_{\text{信息论}=\text{TOED}} \quad \underbrace{\tau(G) \propto D\text{-opt}^{-1}}_{\text{图论}=\text{TOED}} \quad \underbrace{\lambda_2(L) \propto E\text{-opt}}_{\text{图论}=\text{TOED}}
$$